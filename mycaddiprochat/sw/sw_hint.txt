
/* Service Worker Hint: Avoid intercepting Supabase REST to prevent 406 */
self.addEventListener('fetch', (event) => {
  const url = new URL(event.request.url);
  if (url.pathname.startsWith('/rest/v1')) {
    // Let network handle it directly (no cache, no header changes)
    return;
  }
  // ...your existing SW logic
});
